name: main-sequence-final-oldtail-split

on:
  workflow_dispatch:

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired. Use main-sequence-final-v3-no-lookahead.'
